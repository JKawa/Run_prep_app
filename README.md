This app will help you in preparation to next running training.
How to run:

    install docker and docker-compose
    run docker compose up -d
    the app will be available at http://localhost:8501/
